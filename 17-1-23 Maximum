#include <stdio.h>

int main() {
  int arr[5],mini=0;
  
  for(int i=0; i<5;i++){
    printf("\nEnter the %dth element to array : ",i+1);
    scanf("%d",&arr[i]);
  }
  
  for(int i=0; i<5;i++){
    printf("\n%dth element of array is : %d ",i+1,arr[i]);
  }
  
  for(int i=0; i<5; i++){
      
      if(mini<=arr[i]){
          mini = arr[i];
      }
  }
  
  printf("\n%d is the minimum element of Array\n",mini);
  
    return 0;
}
-----------------------------------------------------------------------------
Enter the 1th element to array : 4
Enter the 2th element to array : 10
Enter the 3th element to array : 1
Enter the 4th element to array : 66
Enter the 5th element to array : 7
1th element of array is : 4 
2th element of array is : 10 
3th element of array is : 1 
4th element of array is : 66 
5th element of array is : 7 
66 is the minimum element of Array
